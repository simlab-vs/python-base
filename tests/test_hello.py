"""Tests for the example module. Replace these with tests for your own code."""

from package_name.hello import hello


def test_hello_returns_greeting() -> None:
    """Check that hello returns the expected greeting."""
    assert hello() == "Hello, World!"
